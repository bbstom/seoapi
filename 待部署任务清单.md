# 待部署任务清单

## 任务 1：令牌负载均衡配置保存问题（紧急）

**状态：** 需要调试

**问题：** 保存令牌的负载均衡配置后，刷新页面配置丢失

**已完成：**
- ✅ 数据库添加 `allowed_models` 字段
- ✅ 前端添加 `allowed_models` 字段映射
- ✅ 后端路由添加 `allowedModels` 参数接收
- ✅ 添加调试日志

**需要执行：**
```bash
# 1. 重启后端查看日志
pm2 restart seoapi

# 2. 测试保存配置
# 在浏览器中操作：令牌管理 → 配置节点 → 负载均衡 → 保存

# 3. 查看日志
pm2 logs seoapi --lines 30

# 4. 检查数据库
node scripts/check-token-data.js
```

**参考文档：** `令牌配置问题-操作步骤.md`

---

## 任务 2：数据看板添加API节点列

**状态：** 已完成代码，待部署

**功能：** 在数据看板显示每次调用使用的API节点

**需要执行：**
```bash
# 1. 数据库迁移
cd /www/wwwroot/api.vpno.eu.org/seoapi
node scripts/add-node-info-to-logs.js

# 2. 重启后端
pm2 restart seoapi

# 3. 重新构建前端
cd frontend
npm run build
```

**参考文档：** `数据看板添加API节点列-部署指南.md`

---

## 任务 3：改写模式页面显示模式ID

**状态：** 已完成代码，待部署

**功能：** 在改写模式管理页面显示模式ID（如 `seo_original`），方便与数据看板对应

**修改内容：**
- ✅ 在模式名称旁边显示模式ID（灰色代码标签）
- ✅ 更新说明文字，解释模式ID的用途

**需要执行：**
```bash
# 重新构建前端
cd /www/wwwroot/api.vpno.eu.org/seoapi/frontend
npm run build
```

**效果：**
- 模式卡片显示：`标准改写` `seo_original` `⭐ 反AI检测`
- 用户可以清楚看到数据看板中的 `seo_original` 对应哪个模式

---

## 任务 4：API节点弹窗优化

**状态：** 已完成代码，待部署

**功能：** 将新增/编辑API节点的表单改为弹窗模式，提升用户体验

**修改内容：**
- ✅ 添加全屏遮罩层和居中弹窗
- ✅ 支持点击背景关闭
- ✅ 优化按钮布局和样式
- ✅ 添加弹窗动画效果

**需要执行：**
```bash
# 重新构建前端
cd /www/wwwroot/api.vpno.eu.org/seoapi/frontend
npm run build
```

**效果：**
- 点击"添加节点"显示居中弹窗
- 背景变暗，焦点集中
- 不影响页面布局
- 更符合现代UI设计

**参考文档：** `API节点弹窗优化-说明.md`

---

## 部署顺序建议

1. **先处理任务1**（令牌配置问题）
   - 这是一个bug修复，影响用户使用
   - 需要先调试确认问题根源

2. **再处理任务2**（数据看板 + 节点信息）
   - 需要数据库迁移
   - 需要重启后端

3. **最后处理任务3和任务4**（前端优化）
   - 这两个都是前端修改
   - 可以一起部署

## 统一部署命令（任务2+任务3+任务4）

```bash
# 1. 数据库迁移（任务2）
cd /www/wwwroot/api.vpno.eu.org/seoapi
node scripts/add-allowed-models.js
node scripts/add-node-info-to-logs.js

# 2. 重启后端
pm2 restart seoapi

# 3. 重新构建前端（任务2+任务3+任务4）
cd frontend
npm run build
```

## 注意事项

- 所有操作都需要在生产环境执行
- 重启服务前建议先查看日志确认无错误
- 前端构建不会删除 `.user.ini` 文件
- 建议在低峰期部署
